<UserControl x:Class="Demo_ReportPrinter.Views.PdfPreviewPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:viewModels="clr-namespace:Demo_ReportPrinter.ViewModels"
             xmlns:converters="clr-namespace:Demo_ReportPrinter.Converters"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf">
    <UserControl.Resources>
        <converters:BoolToStringConverter x:Key="BoolToStringConverter"/>
        <converters:BoolToColorConverter x:Key="BoolToColorConverter"/>
    </UserControl.Resources>
    <UserControl.DataContext>
        <viewModels:PdfPreviewViewModel/>
    </UserControl.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- PDF工具栏 -->
        <ToolBar Grid.Row="0">
            <Button Content="刷新" Command="{Binding RefreshPdfCommand}"/>
            <Separator/>
            <Button Content="放大" Command="{Binding ZoomInCommand}"/>
            <Button Content="缩小" Command="{Binding ZoomOutCommand}"/>
            <Separator/>
            <Button Content="打印" Command="{Binding PrintPdfCommand}"/>
            <Button Content="导出" Command="{Binding ExportPdfCommand}"/>
            <Separator/>
            <TextBlock Text="缩放:" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding ZoomLevel, StringFormat={}{0:F1}x}" VerticalAlignment="Center" Margin="5,0,0,0"/>
            <Separator/>
            <TextBlock Text="状态:" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding IsPdfLoaded, Converter={StaticResource BoolToStringConverter}, ConverterParameter=已加载|未加载}" VerticalAlignment="Center" Margin="5,0,0,0" Foreground="{Binding IsPdfLoaded, Converter={StaticResource BoolToColorConverter}, ConverterParameter=Green|Gray}"/>
        </ToolBar>

        <!-- PDF导出选项 -->
        <Border Grid.Row="1" BorderBrush="#E0E0E0" BorderThickness="1" Margin="5" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Label Grid.Row="0" Grid.Column="0" Content="纸张大小:" VerticalAlignment="Center"/>
                <ComboBox Grid.Row="0" Grid.Column="1" ItemsSource="{Binding PaperSizes}" SelectedItem="{Binding SelectedPaperSize, Mode=TwoWay}" Margin="5"/>

                <Label Grid.Row="0" Grid.Column="2" Content="方向:" VerticalAlignment="Center"/>
                <ComboBox Grid.Row="0" Grid.Column="3" ItemsSource="{Binding Orientations}" SelectedItem="{Binding SelectedOrientation, Mode=TwoWay}" Margin="5"/>
            </Grid>
        </Border>

        <!-- 错误消息 -->
        <TextBlock Grid.Row="2" Text="{Binding ErrorMessage}" Foreground="Red" Margin="10" TextWrapping="Wrap"/>

        <!-- WebView2控件 -->
        <wv2:WebView2 Grid.Row="3" x:Name="PdfWebView" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" DefaultBackgroundColor="White" Loaded="PdfWebView_Loaded"/>
    </Grid>
</UserControl>