<Window x:Class="Demo_ReportPrinter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:Demo_ReportPrinter.Views"
        xmlns:viewModels="clr-namespace:Demo_ReportPrinter.ViewModels"
        xmlns:services="clr-namespace:Demo_ReportPrinter.Services.Shared"
        Title="{Binding WindowTitle}"
        Height="768"
        Width="1024"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized">


    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 左侧模板管理树 -->
        <views:TemplateTreePanel Grid.Column="0" Margin="5"/>

        <!-- 右侧主内容区 -->
        <Grid Grid.Column="1" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 工具栏 -->
            <ToolBar Grid.Row="0">
                <Button Content="新建模板" Command="{Binding CreateNewTemplateCommand}"/>
                <Button Content="打开模板" Command="{Binding OpenTemplateCommand}"/>
                <Button Content="保存模板" Command="{Binding SaveTemplateCommand}"/>
                <Separator/>
                <Button Content="生成PDF" Command="{Binding GeneratePdfCommand}"/>
                <Button Content="打印PDF" Command="{Binding PrintPdfCommand}"/>
                <Separator/>
                <Button Content="预览PDF" Command="{Binding PreviewPdfCommand}"/>
            </ToolBar>

            <!-- 内容区 -->
            <TabControl Grid.Row="1">
                <TabItem Header="数据与预览">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <views:DataEntryPanel Grid.Row="0" Margin="0,0,0,5"/>
                        <views:PdfPreviewPanel Grid.Row="1" Margin="0,5,0,0"/>
                    </Grid>
                </TabItem>
                <TabItem Header="模板编辑">
                    <views:TemplateEditorPanel/>
                </TabItem>
            </TabControl>
        </Grid>
    </Grid>
</Window>