# Trae 项目上下文锚点

## 📍 规则库链接
- **全局规则**: @D:\Code\G_Rule\vibe-coding-cn-main\global-loader.md
- **项目画布**: ./canvas/ (如果不存在，自动创建)
- **项目记忆**: ./memory-bank/ (如果不存在，自动创建)

## 🎯 本项目专属约束（覆盖通用规则）
- **技术栈**: （留空，由 AI 询问后填写）
- **模块限制**: （留空，由 AI 询问后填写）
- **可改/不可改**: （留空，由 AI 询问后填写）

## 🚀 启动任务
1. AI 读取本文件后，必须询问："请补充项目技术栈和约束"
2. 自动创建 `canvas/` 和 `memory-bank/` 目录
3. 生成 `memory-bank/@architecture.md` 模板
4. 生成 `memory-bank/@progress.md` 空文件