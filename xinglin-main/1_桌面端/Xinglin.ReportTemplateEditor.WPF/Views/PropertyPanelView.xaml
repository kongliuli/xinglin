<UserControl x:Class="Xinglin.ReportTemplateEditor.WPF.Views.PropertyPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Xinglin.ReportTemplateEditor.WPF.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="300"
             Background="White">
    <Grid>
        <StackPanel>
            <TextBlock Text="控件面板" FontWeight="Bold" FontSize="16" Margin="5"/>
            <TextBlock Text="选中元素：{Binding SelectedElement?.ElementType}" Margin="5"/>
            <Expander Header="位置和大小" IsExpanded="True" Margin="5">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="X：" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding SelectedElement.X, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                    </Grid>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Y：" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding SelectedElement.Y, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                    </Grid>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="宽度：" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding SelectedElement.Width, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                    </Grid>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="高度：" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding SelectedElement.Height, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                    </Grid>
                </StackPanel>
            </Expander>
            <Expander Header="样式" IsExpanded="True" Margin="5">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="背景色：" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding SelectedElement.BackgroundColor, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                    </Grid>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="边框色：" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding SelectedElement.BorderColor, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                    </Grid>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="边框宽度：" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding SelectedElement.BorderWidth, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                    </Grid>
                </StackPanel>
            </Expander>
            <Expander Header="数据绑定" IsExpanded="True" Margin="5">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="数据路径：" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding SelectedElement.DataBinding.Path, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                    </Grid>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="格式：" Grid.Column="0" Margin="5" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding SelectedElement.DataBinding.FormatString, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
                    </Grid>
                </StackPanel>
            </Expander>
        </StackPanel>
    </Grid>
</UserControl>