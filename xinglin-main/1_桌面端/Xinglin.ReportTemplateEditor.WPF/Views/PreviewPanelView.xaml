<UserControl x:Class="Xinglin.ReportTemplateEditor.WPF.Views.PreviewPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Xinglin.ReportTemplateEditor.WPF.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="500"
             Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 工具栏 -->
        <Border Grid.Row="0" Margin="5" Background="#F0F0F0" CornerRadius="3">
            <StackPanel Orientation="Horizontal" Margin="5" Background="#F0F0F0">
            <TextBlock Text="预览界面" FontWeight="Bold" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <Button Content="生成PDF" Command="{Binding GeneratePdfCommand}"/>
            <Slider Minimum="50" Maximum="200" Value="{Binding Zoom}" Width="150" Margin="10,0"/>
            <TextBlock Text="{Binding Zoom, StringFormat={}{0}%}" Margin="5,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>
        
        <!-- 预览区域 -->
        <Border Grid.Row="1" BorderBrush="LightGray" BorderThickness="1" Background="#F8F8F8" Margin="5">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <Grid>
                    <Image Source="{Binding PreviewImage}" 
                           Stretch="Uniform" 
                           Width="{Binding PreviewWidth}" 
                           Height="{Binding PreviewHeight}"/>
                </Grid>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>