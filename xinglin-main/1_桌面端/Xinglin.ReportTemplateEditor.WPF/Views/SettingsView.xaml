<UserControl x:Class="Xinglin.ReportTemplateEditor.WPF.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Xinglin.ReportTemplateEditor.WPF.Views"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 标题 -->
        <TextBlock Grid.Row="0" Text="应用设置" FontWeight="Bold" FontSize="18" Margin="20,10"/>
        
        <!-- 设置内容 -->
        <TabControl Grid.Row="1" Margin="20">
            <!-- 模板设置 -->
            <TabItem Header="模板设置">
                <StackPanel Margin="10">
                    <GroupBox Header="默认页面设置" Margin="0,0,0,20">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="页面宽度：" VerticalAlignment="Center" Margin="5"/>
                            <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" Margin="5">
                                <TextBox Width="100" Text="{Binding DefaultPageWidth}" Margin="0,0,10,0"/>
                                <TextBlock Text="毫米" VerticalAlignment="Center"/>
                            </StackPanel>
                            
                            <TextBlock Grid.Row="1" Grid.Column="0" Text="页面高度：" VerticalAlignment="Center" Margin="5"/>
                            <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="5">
                                <TextBox Width="100" Text="{Binding DefaultPageHeight}" Margin="0,0,10,0"/>
                                <TextBlock Text="毫米" VerticalAlignment="Center"/>
                            </StackPanel>
                            
                            <TextBlock Grid.Row="2" Grid.Column="0" Text="左边距：" VerticalAlignment="Center" Margin="5"/>
                            <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Margin="5">
                                <TextBox Width="100" Text="{Binding DefaultMarginLeft}" Margin="0,0,10,0"/>
                                <TextBlock Text="毫米" VerticalAlignment="Center"/>
                            </StackPanel>
                            
                            <TextBlock Grid.Row="3" Grid.Column="0" Text="右边距：" VerticalAlignment="Center" Margin="5"/>
                            <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal" Margin="5">
                                <TextBox Width="100" Text="{Binding DefaultMarginRight}" Margin="0,0,10,0"/>
                                <TextBlock Text="毫米" VerticalAlignment="Center"/>
                            </StackPanel>
                            
                            <TextBlock Grid.Row="4" Grid.Column="0" Text="上边距：" VerticalAlignment="Center" Margin="5"/>
                            <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal" Margin="5">
                                <TextBox Width="100" Text="{Binding DefaultMarginTop}" Margin="0,0,10,0"/>
                                <TextBlock Text="毫米" VerticalAlignment="Center"/>
                            </StackPanel>
                            
                            <TextBlock Grid.Row="5" Grid.Column="0" Text="下边距：" VerticalAlignment="Center" Margin="5"/>
                            <StackPanel Grid.Row="5" Grid.Column="1" Orientation="Horizontal" Margin="5">
                                <TextBox Width="100" Text="{Binding DefaultMarginBottom}" Margin="0,0,10,0"/>
                                <TextBlock Text="毫米" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Grid>
                    </GroupBox>
                    
                    <GroupBox Header="默认字体设置" Margin="0,0,0,20">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="字体名称：" VerticalAlignment="Center" Margin="5"/>
                            <ComboBox Grid.Row="0" Grid.Column="1" Width="200" Margin="5" 
                                      ItemsSource="{Binding AvailableFonts}" 
                                      SelectedItem="{Binding DefaultFontFamily}"/>
                            
                            <TextBlock Grid.Row="1" Grid.Column="0" Text="字体大小：" VerticalAlignment="Center" Margin="5"/>
                            <TextBox Grid.Row="1" Grid.Column="1" Width="100" Text="{Binding DefaultFontSize}" Margin="5"/>
                            
                            <TextBlock Grid.Row="2" Grid.Column="0" Text="默认颜色：" VerticalAlignment="Center" Margin="5"/>
                            <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Margin="5">
                                <TextBox Width="100" Text="{Binding DefaultFontColor}" Margin="0,0,10,0"/>
                                <Button Content="选择颜色" Width="100"/>
                            </StackPanel>
                        </Grid>
                    </GroupBox>
                </StackPanel>
            </TabItem>
            
            <!-- 渲染设置 -->
            <TabItem Header="渲染设置">
                <StackPanel Margin="10">
                    <GroupBox Header="PDF渲染设置" Margin="0,0,0,20">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="DPI：" VerticalAlignment="Center" Margin="5"/>
                            <TextBox Grid.Row="0" Grid.Column="1" Width="100" Text="{Binding PdfDpi}" Margin="5"/>
                            
                            <TextBlock Grid.Row="1" Grid.Column="0" Text="压缩质量：" VerticalAlignment="Center" Margin="5"/>
                            <Slider Grid.Row="1" Grid.Column="1" Minimum="0" Maximum="100" Value="{Binding PdfCompressionQuality}" Width="200" Margin="5"/>
                        </Grid>
                    </GroupBox>
                </StackPanel>
            </TabItem>
            
            <!-- 界面设置 -->
            <TabItem Header="界面设置">
                <StackPanel Margin="10">
                    <GroupBox Header="主题设置" Margin="0,0,0,20">
                        <StackPanel>
                            <RadioButton Content="浅色主题" IsChecked="{Binding IsLightTheme}" Margin="5"/>
                            <RadioButton Content="深色主题" IsChecked="{Binding IsDarkTheme}" Margin="5"/>
                        </StackPanel>
                    </GroupBox>
                    
                    <GroupBox Header="语言设置" Margin="0,0,0,20">
                        <ComboBox Width="200" Margin="5" 
                                  ItemsSource="{Binding AvailableLanguages}" 
                                  SelectedItem="{Binding SelectedLanguage}"/>
                    </GroupBox>
                </StackPanel>
            </TabItem>
        </TabControl>
        
        <!-- 底部按钮 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="20">
            <Button Content="应用" Width="100" Margin="5" Command="{Binding ApplyCommand}"/>
            <Button Content="保存" Width="100" Margin="5" Command="{Binding SaveCommand}"/>
            <Button Content="取消" Width="100" Margin="5" Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>